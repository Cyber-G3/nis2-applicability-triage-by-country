# Next engineering task

Implement the versioned applicability decision record as a first-class export/API object and add regression fixtures for Spain plus at least one `planned` and one `pending` country to prove verification status cannot be lost downstream.
